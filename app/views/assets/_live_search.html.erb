<% @assets.group_by(&:content_type).each do |asset_type, assets|  %>
	<%= render :partial => "#{asset_type.pluralize.downcase}/live_search", :locals => {:assets => assets.map(&:content)} rescue nil %>
<% end %>
