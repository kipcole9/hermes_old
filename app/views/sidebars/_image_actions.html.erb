<h4>Actions:</h4>
<ul>
	<li>
		<%= link_to "#{image_tag("/images/icons/photo.png")} Show images in Google Maps", 
					"http://maps.google.com/?q=" + h(formatted_images_url(:kml, sanitize_params(params))) %>
	</li>
	<li>
		<%= link_to "#{image_tag("/images/icons/photo.png")} Download kml file for these images", 
					h(formatted_images_url(:kml, sanitize_params(params))) %>
	</li>
	<li>
		<%= link_to "#{image_tag("/images/icons/photo.png")} Subscribe to Photofeed for these images", 
					h(formatted_images_url(:rss, sanitize_params(params))) %>
	</li>	
</ul>	

	
